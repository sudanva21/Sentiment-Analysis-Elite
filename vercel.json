{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/index.py"
        }
    ],
    "env": {
        "ENABLE_PIP_INSTALL": "1"
    },
    "installCommand": "pip install -r requirements.txt"
}